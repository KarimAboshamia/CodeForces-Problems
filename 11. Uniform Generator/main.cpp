#include <iostream>

using namespace std;

function seed(int seedNum, int step, int mood)
{
    return seed(seedNum-1)
}

int main()
{
    int index = 0, mod, step;
    while(cin>>step >> mod)
    {

    }

    return 0;
}
